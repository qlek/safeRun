<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.specjalista_it.bezpiecznyprzebieg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/start_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LuncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondListActivity"></activity>
        <activity android:name=".FirstListActivity"></activity>
        <activity android:name=".InfoActivity"></activity>
        <activity android:name=".ThirdListActivity"></activity>
        <activity android:name=".DetailInfoActivity"></activity>
    </application>

</manifest>
